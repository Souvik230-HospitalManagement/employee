<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Employee</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="container mt-5">
        <h2>Edit Employee</h2>
        <form action="/edit-emp/<%= employee.emp_id %>" method="POST">
            <div class="form-group">
                <label for="deperment_name">Department Name</label>
                <input type="text" class="form-control" id="deperment_name" name="deperment_name" value="<%= employee.deperment_name %>" required>
            </div>
            <div class="form-group">
                <label for="emp_name">Employee Name</label>
                <input type="text" class="form-control" id="emp_name" name="emp_name" value="<%= employee.emp_name %>" required>
            </div>
            <div class="form-group">
                <label for="role">Role</label>
                <input type="text" class="form-control" id="role" name="role" value="<%= employee.role %>" required>
            </div>
            <button type="submit" class="btn btn-primary">Update</button>
        </form>
    </div>
</body>
</html>
